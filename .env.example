# =============================================================================
# Plantilla de variables. Copia este archivo a `.env` y pon claves reales.
#   PowerShell:  Copy-Item .env.example .env
# Este SI se sube a Git; el `.env` NO.
# =============================================================================

# --- MySQL 8.4 -------------------------------------------------------------
MYSQL_ROOT_PASSWORD=cambia-esta-clave-root
MYSQL_DATABASE=colorsin_inventario
MYSQL_USER=colorsin
MYSQL_PASSWORD=cambia-esta-clave-app
